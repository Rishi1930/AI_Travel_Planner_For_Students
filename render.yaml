services:
  - type: web
    name: ai-trip-planner
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: GOOGLE_API_KEY
        sync: false
